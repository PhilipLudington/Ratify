node_modules/
dist/
.wrangler/

# Generated from wrangler.log.toml by `npm run types` (run-build.sh does it
# for you). Derived, ~15k lines, and stale the moment the config changes —
# so it is regenerated rather than committed.
worker-configuration.d.ts

# AirTower results are machine state, regenerated by the wrapper scripts.
.test-results.json
.build-results.json
.vitest-report.json

# Local secrets. The demo passphrase and API key never enter this repo.
.dev.vars
.env
.env.*

.DS_Store
*.log
